---
description: Padrões TypeScript e React
globs: "**/*.{ts,tsx}"
alwaysApply: false
---

# TypeScript e React

- Componentes funcionais. Hooks para lógica reutilizável.
- Importar Supabase de `@/lib/supabase/client` ou `@/lib/supabase`.
- Páginas dinâmicas com auth: `export const dynamic = 'force-dynamic'`.
- Tratamento de erro: logar e propagar; evitar `catch (e) {}` vazio.
